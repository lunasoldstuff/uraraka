.rp-media-imgur(ng-switch="imgurType")
	.rp-media-imgur-image-switch(ng-switch-when="image")
		//- img.rp-media-image.rp-media-imgur-image.rp-resize-drag(ng-src="{{imageUrl | rpMediaHttpsFilter}}")
		img.rp-media-image.rp-media-imgur-image(ng-src="{{imageUrl | rpMediaHttpsFilter}}", ng-click="openImagePanel()")

	.rp-media-imgur-video-switch(ng-switch-when="video")
		.rp-media-imgur-video-thumbnail-wrapper(ng-if="!showGif && !slideshow")
			img.rp-media-thumbnail.rp-media-imgur-gif-thumbnail(ng-src="{{thumbnailUrl | rpMediaHttpsFilter}}")
			md-icon.rp-media-imgur-play-icon(md-svg-icon="ic_play_circle_outline_48px", ng-click="show()", aria-label="play")

		.rp-media-imgur-video-wrapper(ng-if="showGif || !!slideshow")
			video.rp-media-image.rp-media-imgur-video(autoplay, loop, muted, preload)
				source(ng-src="{{webmUrl | rpMediaHttpsFilter | rpMediaTrustedResourceFilter}}", type="video/webm")
				source(ng-src="{{mp4Url | rpMediaHttpsFilter | rpMediaTrustedResourceFilter}}", type="video/mp4")
			md-icon.rp-media-imgur-pause-icon(ng-if="!slideshow", md-svg-icon="ic_pause_circle_outline_48px", aria-label="pause", ng-click="hide()")
