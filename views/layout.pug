doctype html
html(
  lang="en"
  ng-app="rpApp"
  ng-controller="rpAppCtrl"
  md-theme="{{settings.theme}}"
  ng-class="{'rp-animation-false': !settings.animations}"
  authenticated=`${authenticated}`
  user-agent=`${userAgent}`
  error=`${error}`
  ng-init="init()"

)
  include head.pug

//- p user-agent: #{userAgent}
//- p error: #{error}
//- p message: #{message}
//- class=""

body.rp-body(
  layout="column"
  ng-class="[settings.fontSize, {'rp-night-theme': !!settings.nightTheme}]"
)

  block facebook
  block content
